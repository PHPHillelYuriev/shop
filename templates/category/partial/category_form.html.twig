{% form_theme form 'bootstrap_4_layout.html.twig' %}

{{ form_start(form) }}

    {{ form_row(form.name) }}
    {{ form_row(form.parent) }}
    <button type="submit" class="btn btn-primary btn-block">{{ button_label|default('Добавить категорию') }}</button>

{{ form_end(form) }}