{{ dump(resultProductsInfo) }}
{% if resultProductsInfo is empty %}
<div class="cart_section">
    <div class="container">
        <div class="row">
            <div class="col-lg-10 offset-lg-1">
                <div class="cart_container">
                    <div class="cart_title">Shopping Cart is empty</div>
                </div>
            </div>
        </div>
    </div>
</div>
{% else %}                        
<div class="cart_section">
    <div class="container">
        <div class="row">
            <div class="col-lg-10 offset-lg-1">
                <div class="cart_container">
                    <div class="cart_title">Shopping Cart</div>
                    <div class="cart_items">
                        <ul class="cart_list">
                            {% for productInfo in resultProductsInfo%}
                            {% set product = productInfo['product'] %}
                            {% set quantity = productInfo['quantity'] %}
                            <li class="cart_item clearfix">
                                <div class="cart_item_image"><img src="{{ asset('images/shopping_cart.jpg') }}" alt=""></div>
                                <div class="cart_item_info d-flex flex-md-row flex-column justify-content-between">
                                    <div class="cart_item_name cart_info_col">
                                        <div class="cart_item_title">Name</div>
                                        <div class="cart_item_text">{{ product.manufacturer }} {{ product.model }}</div>
                                    </div>
                                    {# <div class="cart_item_color cart_info_col">
                                        <div class="cart_item_title">Color</div>
                                        <div class="cart_item_text"><span style="background-color:#999999;"></span>Silver</div>
                                    </div> #}
                                    <div class="cart_item_quantity cart_info_col">
                                        <div class="cart_item_title">Quantity</div>
                                        <div class="cart_item_text">{{ quantity }}</div>
                                    </div>
                                    <div class="cart_item_price cart_info_col">
                                        <div class="cart_item_title">Price</div>
                                        <div class="cart_item_text">{{ product.price }}</div>
                                    </div>
                                    <div class="cart_item_total cart_info_col">
                                        <div class="cart_item_title">Total</div>
                                        <div class="cart_item_text">${{ product.price * quantity }}</div>
                                    </div>
                                </div>
                            </li>
                            {% endfor %}
                        </ul>
                    </div>
                    
                    <!-- Order Total -->
                    <div class="order_total">
                        <div class="order_total_content text-md-right">
                            <div class="order_total_title">Order Total:</div>
                            {% set price = 0 %}
                            {% for productInfo in resultProductsInfo %}
                                {% set price = price + (productInfo['product'].price) * (productInfo['quantity']) %}                            
                                
                            {% endfor %}
                            <div class="order_total_amount">${{ price }}</div>
                        </div>
                    </div>

                    <div class="cart_buttons">
                        <a href="{{ path('claerCart') }}" class="button cart_button_clear">Clear Cart</a>
                        <button type="button" class="button cart_button_checkout">Add to Cart</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}